# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Environment & secrets
.env
.env.*
*.secret
secrets/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# Build / dist
dist/
build/
*.egg-info/

# Test / coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# PDR extraction working directories (zips are canonical)
.01_PDRs/extracted/

# Agent runtime logs (JSONL entries; not source-controlled by default)
.agents/logs/*.jsonl
# PDR intake runtime state (SQLite + JSONL)
.01_PDRs/.intake/*.db
.01_PDRs/.intake/*.jsonl
# Review intake: lock files and runtime logs are transient; findings log and queue are tracked
.agents/review-intake/locks/*.json
.agents/review-intake/logs/*.jsonl
# Keep the directory itself tracked via .gitkeep — do not ignore the dir
