# Tailwind System Guide

Use Tailwind when speed, consistency, and utility-first composition matter. Create a shared preset for tokens and a component recipe layer for repeated patterns.

Include theme tokens, plugin choices, class naming recipes, component variants, responsive conventions, linting/formatting guidance, and an escape hatch policy for custom CSS.
