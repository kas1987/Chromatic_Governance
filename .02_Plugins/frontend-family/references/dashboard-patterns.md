# Dashboard Patterns

Dashboards should answer a decision question, not merely display charts.

Common layouts: KPI strip plus detail table, status board with filters, incident/control-room view, CRUD admin panel, project health cockpit, model/eval monitoring view.

Always define refresh cadence, owner, data source, confidence, and action path.
