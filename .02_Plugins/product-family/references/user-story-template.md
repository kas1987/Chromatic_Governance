# User Story Template

## Story

Title: `<short action-oriented title>`

As a `<user or role>`, I want `<capability>`, so that `<outcome>`.

## Context

- User segment:
- Problem:
- Evidence:
- Current workaround:

## Acceptance notes

- Given `<context>`, when `<action>`, then `<observable result>`.
- Given `<edge case>`, when `<action>`, then `<safe result>`.

## Boundaries

- In scope:
- Out of scope:
- Dependencies:
- Assumptions:
- Open questions:

## Split check

Split the story if it contains multiple user outcomes, multiple personas, multiple release gates, or high-risk platform changes.
